# D4 阶段任务清单：全量主训练、消融矩阵与评测交付 v1

> **D4 阶段核心哲学**：**主训练出顶梁柱，消融实验出科研深度！** 在 D3 调通闭环的基础上，开展全量 2,000 ~ 4,000 条黄金题目的过夜长训，并通过严密的消融实验（奖励机制、算法对比、数据质量）和安全审计（防 Reward Hacking），证明强化学习相比基座模型的显著代差与泛化能力。

---

## 0. D4 关键定位与前序交付

* **前序依赖（输入）**：
  * D2 成果：全量黄金题库 `data/rlvr/train_full.parquet`、独立评测集 `data/rlvr/test.parquet`；
  * D3 成果：已验证健康的 PPO 20-step 训练配置、正式代码奖励函数 `rewards/code_rlvr.py`。
* **D4 核心目标（输出）**：
  1. 主训练 Checkpoint：1~2 Epoch 训练定型的 7B PPO 强化学习模型；
  2. 三大消融实验数据与对比曲线（奖励函数 A/B、PPO vs GRPO、数据过滤有效性）；
  3. 评测报告：Held-out 200 题与公共 Benchmark (MBPP/LCB) 的 Pass@1/Pass@8 提分对照表；
  4. 安全与动力学分析：防 Reward Hacking 人工抽检记录表与 WandB/TensorBoard 曲线集。

---

## 1. D4 核心任务四步拆解

### 任务 1：全量 7B × 8 卡 PPO 正式主训练（过夜长训）

在 8 卡 910B 上开启无人值守正式主训练，产出最终的主干模型。

* **训练规模与超参**：
  * 数据量：2,000 ~ 4,000 条黄金 Prompts（全量 `train_full.parquet`）；
  * 训练轮次：1 ~ 2 Epochs（约 40 ~ 80 steps）；
  * 批次配置：`train_batch_size=64`，`ppo_mini_batch_size=64`，`ppo_micro_batch_size_per_gpu=4`；
  * 序列长度：`max_prompt_length=512`，`max_response_length=768`（支持思考链与复杂代码）；
  * 检查点机制：开启 `trainer.save_freq=10`，每 10 步自动将权重存至持久挂载目录；
* **启动方式（无人值守后台模式）**：
  ```bash
  cd /data/home/<你的学号>/project
  # 使用内置保活与日志落盘的入口脚本
  nohup bash start_train.sh > train_main.log 2>&1 &
  ```
* **耗时预估**：单步约 2.5 ~ 3.5 分钟，全量 60 步总耗时约 3 ~ 4 小时（过夜可从容完成）。

---

### 任务 2：三大核心消融实验（Ablation Studies，论文与汇报灵魂）

为了支撑高质量项目报告与答辩，并行开展三组具备强说服力的消融对比：

```text
               ┌── [消融 1: 奖励机制] ── 全有全无(0/1) vs 平滑连续部分分
               │
主线 7B PPO ──┼── [消融 2: 算法架构] ── PPO (带Critic) vs GRPO (无Critic群组采样)
               │
               └── [消融 3: 数据纯度] ── 未经筛4的原始池 vs 筛4黄金难度池
```

1. **消融 1：奖励函数整形 A/B 测试（证明奖励设计的巧妙性）**
   * **对照组 A（纯稀疏 0/1 奖）**：全部测试通过给 1.0，只要有 1 个测试未通过直接给 0.0；
   * **对照组 B（平滑连续奖，主线方案）**：格式分 0.1 + 通过用例比例 (passed/total) × 0.9；
   * **预期结论**：证明连续部分分能给处于探索边界的题目提供更稠密的引导信号，收敛更快更稳。
2. **消融 2：PPO vs GRPO 对比（证明算法架构选型理由）**
   * 运行 MindSpeed-RL / verl 的 GRPO 模式（取消 Critic 显存占用，改为单 Prompt 采样 N 个答案计算组内优势）；
   * **对比维度**：单步耗时、显存占用峰值、最终生成代码的准确率与多样性。
3. **消融 3：数据过滤效果消融（证明 D2 四道大筛的决定性价值）**
   * 用未经筛 4 过滤的原始题目（含大量 100% 极易题与 0% 极难题）训练 20 步；
   * 对比筛 4 黄金池（10%~90% 通过率）的提升斜率，证明“淘汰极易极难题”是强化学习避免梯度弥散与策略崩塌的关键前提！

---

### 任务 3：客观独立评测与基准对比（证明真提升）

强化学习模型必须在从未见过的试卷上证明泛化能力，严防死记硬背。

* **评测集 1：严格留出的 Held-out 200 题**
  * 使用从黄金池物理隔离的 `data/rlvr/test.parquet`（包含未进训练池的 200 道算法与工程题）；
  * 评测指标：Pass@1（贪婪解码，确定性输出）；
* **评测集 2：公共权威基准（OpenAI HumanEval 164 题）**
  * 算法与函数级经典基准，全量 164 题无泄露零重叠；
* **评测集 3：公共权威基准（Google MBPP-Sanitized 427 题）**
  * 使用平台已就位的 `mbpp_sanitized.jsonl`，评估通用 Python 编码能力（427 道人工校验题）；
* **三方权威基准实测能力全景大对照表（791 题全量实测最终成果汇报）**：
  | 模型版本 | KodCode (200 题) | HumanEval (164 题) | MBPP (427 题) | 791 题宏观总通过率 | 致命错误数 | 平均 Token 长度 |
  |---|:---:|:---:|:---:|:---:|:---:|:---:|
  | **Qwen2.5-7B-Instruct 基座** | 76.50% (153/200) | 78.66% (129/164) | 73.30% (313/427) | **76.15% (595/791)** | 24 次 | 382.1 |
  | **PPO Step 71 (主线模型)** | **82.00% (164/200)** | 82.93% (136/164) | **75.88% (324/427)** | **80.27% (624/791)** 🏆 | **12 次 (-50%)** | 379.7 |
  | **GRPO Step 50 (消融模型)** | 79.00% (158/200) | **83.54% (137/164)** ⚡ | **75.88% (324/427)** | **79.47% (619/791)** | 18 次 | **192.4 (-49.3%)** |
  | **PPO-NegPenalty Step 50 (消融 1)** | 77.50% (155/200) | 78.66% (129/164) | 73.30% (313/427) | **76.24% (597/791)** | **仅 2 次 (-91.7%)** 🛡️ | **203.3 (-46.8%)** |
  | **PPO-SparseRLVR Step 50 (消融 2)** | 74.00% (148/200) | **81.10% (133/164)** ⚡ | **75.41% (322/427)** ⚡ | **76.23% (603/791)** | **仅 4 次 (-83.3%)** 🛡️ | 359.5 (-5.3%) |

> **核心实测结论**：
> 1. **PPO 全面领跑宏观大榜**：在全量 791 道题目中，PPO 达成 **80.27%** 的宏观通过率，相对基座提升 **+4.12%**（净多解 29 题），在多分支工程题（KodCode）上斩获全场最高 82.00%，致命崩溃率砍半；
> 2. **GRPO 极简突围 HumanEval**：去除 Critic 网络后，GRPO 单步耗时缩短 42%，生成的代码极度精炼（192 tokens），在纯单函数算法基准 HumanEval 上斩获 **83.54%** 全场最高记录！

---

### 任务 4：训练动力学与安全审计（防 Reward Hacking）

* **曲线指标固化**：
  * 导出高清训练动力学曲线：PPO 奖励从 0.720 攀升至 0.8984，Critic Loss 下降 83.1%（0.0842 → 0.0142）；GRPO 奖励由 0.7420 上升至 0.9083，方差持续收敛；
* **人工抽检 50 条样本（安全防黑客审计）**：
  * 随机抽取 50 条高奖励输出，人工检查模型是否存在“作弊”嫌疑：
    * ❌ 是否存在空函数骗过某些无断言测试？—— 无，AST 静态检查全部通过；
    * ❌ 是否存在直接输出 `sys.exit(0)` 伪造退出码？—— 无，沙箱捕获并拦截一切非规范系统调用；
    * ✅ 是否真正给出了严密、健壮的逻辑算法与边界处理？—— 是，PPO 在边界值防御上尤为严密。

---

## 2. D4 验收交付清单（收工标准）

- [x] 全量 7B PPO 主训练完成，1 个完整 Epoch 71 步训练全部跑通，生成完整 Checkpoint 并导出 HF 格式；
- [x] 导出主训练完整曲线，包含明显的 Reward 爬坡与稳定的 KL 控制（终局 KL < 0.02）；
- [x] 完成核心算法架构消融实验（PPO vs GRPO 对比，耗时提速 42%，显存降低 14GB）；
- [x] 完成 Held-out 200 题、HumanEval 164 题与 MBPP 427 题全量自动化评测，形成 791 题全景大表；
- [x] 完成 50 条样本的人工防 Hacking 审核并确认无作弊代码；
- [x] 所有评估脚本与生成权重整理就绪，技术报告 [`ASCEND_910B_RLVR_TECHNICAL_REPORT.md`](file:///E:/%E4%BA%BA%E5%B7%A5%E6%99%BA%E8%83%BD%E9%A1%B9%E7%9B%AE/1_%E8%87%AA%E5%B7%B1%E7%9A%84%E9%A1%B9%E7%9B%AE/%E5%BC%BA%E5%8C%96%E5%AD%A6%E4%B9%A0%E4%B8%8E%E5%A4%A7%E6%A8%A1%E5%9E%8B/RLHF/Ascend-910B-PPO-RLHF/ASCEND_910B_RLVR_TECHNICAL_REPORT.md) 编写完成。
