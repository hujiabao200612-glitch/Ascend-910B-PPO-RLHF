# D1 开工清单 v3（模块 A · 北理工 SCOW AI 平台适配版）

> 平台：SCOW AI（k8s + 容器）。**容器即焚，只有挂载点保留**（官方明确）。挂载点环境变量：算法 `SCOW_AI_ALGORITHM_PATH`、数据集 `SCOW_AI_DATASET_PATH`、模型 `SCOW_AI_MODEL_PATH`。
> 队列 gpu-queue（8 节点 × 8 卡 910B3）。单作业 1~8 卡。今天唯一主线：让 RL 链路在平台上完整跑出训练曲线。

> **进度快照（2026-09-14）**：主线已达成 ✅——环境路线定型（官方 vllm-ascend 0.9.1rc1 镜像 + verl 0.6.1，§1 离线 wheel 方案废弃未用）；0.5B PPO 冒烟 4 步全通；7B 单卡推理 + vllm A/B 验证通过；两模型 + mbpp 已上服务器。**剩余主线：判分/数据管线（D2，本地 CPU）→ 8 卡冒烟（命令见平台操作SOP 附 A）→ 8 卡正式训练**。

## 0. 平台摸底（今天必须拿到答案的六件事）

- [x] **① 作业时长上限（已确认）**：最长 **1 天（24h）**——主训练 1~2 epoch 约 2~10h，单作业内可完成，过夜无碍；60 分钟只是 VSCode 默认值，记得手动调大；万一超 24h 用 checkpoint 接力换天续跑
- [x] **② 网络（2026-09-12 实测：容器有网，gitee 200 OK）**——框架与依赖可在线安装；模型/数据已本地化完毕，无需重下
- [x] **③ 默认镜像环境（已查明）**：镜像 `ascend-vscode:4.20.0.1-cann8.0.rc2-torch2.3.1_new`，容器内 **torch 2.3.1 + torch_npu 2.3.1（`torch.npu.is_available()=True`）+ transformers 4.41.2 + accelerate 0.30.1**，无 vllm/deepspeed/mindspeed；**容器有网（gitee 200 OK）**
- [x] **⑦ 关键发现（2026-09-12 晚，已修正）**：默认镜像 `cann8.0.rc2-torch2.3.1` 跑不了 RL 框架——MindSpeed-RL 全版本要求 ≥ torch 2.5.1 + CANN 8.1.RC1 + Python 3.10。**但驱动 24.1.rc2 官方兼容 CANN 8.1.RC1**（CANN 8.1.RC1 商用版兼容性表含 HDK 24.1.RC2）→ **结论：不用换镜像、不用动驱动，容器内原地升级**（CANN 8.1.RC1 装到挂载目录 + torch 2.5.1 + conda py3.10），配套 **MindSpeed-RL 2.0.0**。一键脚本：`setup_env_msrl.sh`（项目根目录，每个新作业开头执行一次）。备选：远程镜像拉 CANN 8.1.RC1 + torch 2.5.1 成套镜像。
- [x] **④ 环境持久化（已确认）**：挂载点 + pip `--target` 装进挂载目录，做法已掌握
- [x] **⑤ 多卡并行（已确认）**：每人每天可独占 8×910B3，四人 = 4×8 卡并行 → 主训练与三组消融同时跑，无需排队；无跨节点，长训练用 checkpoint 接力换人续跑。
- [ ] **⑥「训练」作业提交方式**：左侧「训练」栏和 VSCode 应用有什么区别（时长/卡数/能否挂载全部三种资源），冒烟通过后正式训练要从这里走。

产出：服务器现状表（含 CANN/torch_npu 版本、网络、时长上限、持久化方式、配额）。

## 1. 环境安装策略（**已废弃 2026-09-13**：改为官方 vllm-ascend 0.9.1rc1 镜像 + 持久 venv，见平台操作SOP 第 1/4 步——以下留档仅作历史）

指南背书：DeepSpeed / Accelerate / LLaMA-Factory 原生支持昇腾 NPU——冷启动 SFT 直接用 LLaMA-Factory。

离线安装三步：

1. **查版本**（此步不需要网络）：进默认镜像 VSCode，抄下 CANN 版本、`python3 --version`、已装 torch / torch_npu 版本（`pip list | grep -E "torch|npu"`）；
2. **本地拉 wheel**（Windows 上跨平台下载，注意 `--python-version` 必须与容器内一致）：
   ```bash
   pip download -d wheelhouse/ -r requirements.txt \
     --platform manylinux2014_x86_64 --python-version 3.10 --only-binary=:all:
   ```
   torch_npu 的 wheel 去 `gitee.com/ascend/pytorch` 按**集群 CANN 版本**配对下载，不要乱配；
3. **上传 wheelhouse → 容器内离线安装**：
   ```bash
   pip install --no-index --find-links=$SCOW_AI_DATASET_PATH/wheelhouse \
     -r requirements.txt --target=$SCOW_AI_DATASET_PATH/pylibs
   ```
   之后每个作业开头：`export PYTHONPATH=$SCOW_AI_DATASET_PATH/pylibs:$PYTHONPATH`。

验收：`python -c "import torch, torch_npu; print(torch.npu.is_available())"` → True；`import mindspeed`（或 verl）→ 无报错。

## 2. 模型与数据（有网就现下，没网才上传）

- [x] 模型上服务器（2026-09-14 验证）：`Qwen2.5-0.5B-Instruct` + `Qwen2.5-7B-Instruct`（本地 15.2GB md5 校验后上传；7B 实测可被 HF 与 vllm-ascend 双引擎正常加载）
- [x] `mbpp_sanitized.jsonl` 已上服务器（冒烟即用）；KodCode 候选 1.5 万条在本地 `data_set\`，筛 1~3 跑完传 `verified_pool.jsonl`
- [x] 存放到挂载点：模型 `/data/home/<学号>/`（建作业时逐个文件夹挂载），数据/代码 `/data/home/<学号>/project/`
- 上传兜底：没网时走「文件管理」网页上传或 scp（本地 15.2GB 已校验，上传前 `md5sum -c checksum.md5`）

## 3. 冒烟（两段式，先易后难）

- [x] **第一段（已完成 2026-09-12）**：0.5B 模型 NPU 推理冒烟通过——模型输出正常中文；挂载真实路径为 `/data/home/<你的学号>/`（按学号，非作业页显示的 f<你的学号>）；**挂载按单文件夹逐个挂，父目录下新建文件夹不持久**，代码/日志目录需在建作业时单独添加挂载点
- [x] **第二段（RL 链路，2026-09-14 全部通过 ✅）**：最终路线定型 = **verl 0.6.1 + 官方 vllm-ascend 0.9.1rc1 镜像**（替代 MindSpeed-RL 与 verl 0.4.1 方案）。冒烟实测：**PPO 训练循环在 910B3 上完整跑通 4 步**——Ray/Worker 启动、Critic+Actor 加载进 NPU、vllm-ascend 生成、奖励计算（rewards max 1.0）、全量指标输出、验证生成，全链路 ✓。关键结论：① 平台默认镜像的 CANN 8.0.RC2 + torch 2.3.1 跑不了 verl；② **镜像原生 CANN 8.1.rc1 是唯一正确选择**——往容器叠加第二套 CANN（挂载版 8.1.RC1）导致所有算子报错（实测复现）；③ 已知小坑与对策：dpc 网络盘 pip 装 pyc 断言失败（`PIP_NO_COMPILE=1` 解决）、verl 0.6.1 的 gsm8k.py 与 naive 管理器签名不一致（自写 wrapper 解决）、**约 2/3 回答撞 256 token 截断**（正式训练 max_response_length 提到 512+）。verl 0.6.1 需 transformers≥4.52（已升 4.52.4，MindSpeed-RL 备选路线因此失效——主路线 verl 不受影响）。
- [x] 通过标准（2026-09-14）：rewards/mean 0.0156、max 1.0 有波动，4 步全程无 NaN；checkpoint 写盘留到正式训练开 `trainer.save_freq>0` 时一并验证
- 原则不变：零修改，谁提改奖励就打谁

## 4. 7B 实测（冒烟通过后）

- [x] **rollout 引擎 A/B 实测 ✅（2026-09-14，风险解除）**：vllm-ascend 0.9.1rc1 对 Qwen2.5-7B 完全可用——引擎加载 33.2s、生成 316 tok/s、输出正确 Python 代码、权重 14.25GB + 2327 KV blocks（单卡 0.6 util）。平台指南「vLLM 在昇腾表现不佳」是旧结论，**MindIE / HF-generate 预案不触发**；正式配置敲定 rollout TP=1、gpu_memory_utilization 0.4~0.5。数据详见平台操作SOP 附 A
- [ ] 8 卡 PPO 小规模（128 prompts × 2~5 step）：显存峰值、单步耗时 → 回填计划 §8（**命令已备好、不依赖数据集**：平台操作SOP 附 A「8 卡冒烟两连」，0.5B×8卡 ~10min + 7B×8卡 ~30min）

**rollout 引擎预案**（若 vllm-ascend 确实不行）：HF generate 慢速 rollout（吞吐 3~5 折，日程 +1 天）或 MindIE 推理服务（指南称性能最好，兼容 OpenAI API，需实测接入成本）。

## 5. 收工 Go/No-Go

- **Go（2026-09-14 达成 ✅）**：NPU 推理通 + RL 链路通 + 24h 时长够用 → 已转 D2 数据与判分器（判分器开发纯本地 CPU 不占卡；8 卡冒烟随时可跑，与数据集无关）
- **No-Go 路径**：MindSpeed-RL 堵 → verl；RL 框架全堵 → 极端兜底：LLaMA-Factory（平台背书）先跑 SFT 交付部分 + RL 用最小自写 PPO 循环（最后一道防线，尽量别走到这）
- 服务器现状表 + 冒烟日志发群

## 今日避坑

1. 时长上限（①）是头号待办，10 分钟内要有答案；
2. 遇「算子不支持」类报错 = CANN 缺算子（指南 §4.4）：先查框架 issues，再昇腾论坛，报错原文存档；
3. 一切要保留的东西（代码、pip 包装的依赖、checkpoint、日志）**只写挂载点**，写容器其他路径 = 作业结束蒸发；
4. 推理服务（「推理」栏）默认仅集群内访问，外部访问要管理员开防火墙——评测阶段用得上，先记着；
5. 两人并行：各自独立 VSCode 容器，pip 装各自挂载目录。
