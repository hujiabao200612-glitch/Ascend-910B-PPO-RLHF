# D2 数据准备清单 v2（模块 A · 代码 RLVR）

> **先记住一张表**：筛 1~3 + 判分器开发**全部在本地跑**（纯 CPU，Windows 可）；只有筛 4 要上服务器（7B 采样）。
> **判分器 ≠ 奖励函数**：判分器是客观内核（先写先冻结）；奖励设计（整形层）D3/D4 敲定、随时可换——这就是"奖励函数先不敲定"能成立的原因。

> **进度快照（2026-09-14）**：① 判分器/沙箱需求已写成 AI 任务书（`D2_代码任务书_给AI生成.md`，六个交付物：config / sandbox / extract / score / 三过滤器 / 100 条边界单测）；② 筛 4 需要的 7B 已上服务器并验证通过（vllm-ascend 316 tok/s）；③ KodCode 1.5 万条候选原料就位。**当前唯一卡点 = 判分器代码本身（本地 CPU 活，不占卡）**——判分器不写完，筛 2 / 筛 4 / RL 奖励全动不了；服务器侧无等待事项（8 卡冒烟与数据集无关，见平台操作SOP 附 A）。

---

## 0. 执行位置总表

| 步骤 | 在哪跑 | 需要什么 | 备注 |
|---|---|---|---|
| 判分器 + 沙箱开发 | 本地 | Python + pytest | 纯写代码 |
| 判分器 100 条边界单测 | 本地 | Python | 判分器的验收件 |
| 筛 1：模板重排 | 本地 | CPU，秒级 | 文本处理 |
| 筛 2：判分可行性验证 | 本地 | 官方解答 + 沙箱（轻隔离够用） | **不需要模型** |
| 筛 3：去重截长 | 本地 | CPU | 文本处理 |
| 筛 4：难度过滤 | **服务器 1 卡** | 7B 模型 + GPU | 模型唯一出场的清洗步骤 |
| 奖励组装（整形层）设计 | 本地，纸面 | —— | D3/D4 敲定，消融对象 |

---

## 1. 已就位的数据（本地 `data_set\`，全部验证过）

| 文件 | 内容 | 状态 |
|---|---|---|
| `data\mbpp_sanitized.jsonl` | MBPP 420 题（train 120 / test 257 / val 43），字段 task_id/prompt/code/test_list | ✅ 冒烟即用 |
| `data\kodcode_candidates.jsonl` | KodCode-V1 随机 15,000 条候选（来自 2 分片共 6.4 万行） | ✅ 筛 1~3 的原料 |
| `data\kodcode_v1\data\*.parquet` | 原始分片 2 个 | 原料留存，rl_pool 定稿后可删 |
| `benchmark\lcb\test6.jsonl` | LCB 最新时间窗（134MB） | 评测抽题原料 |
| `benchmark\mbppplus\`、`benchmark\hepplus\` | 辅评集 | ✅ |

KodCode 关键字段：`question`（题面）｜`solution`（官方解答——筛 2 验证测试用）｜`test`（pytest 单测，`from solution import ...`）｜`subset`/`style`｜`gpt_difficulty`（现成难度标签）｜`benchmark_similarity`（**>0.9 建议过滤**，防泄漏）。

---

## 2. 判分器 vs 奖励函数（解耦说明）

```
奖励函数 = 判分器（内核 core） + 奖励组装（shaping + penalty）

判分器   = 客观事实层：提取代码 → 沙箱跑测试 → 报告通过率（无设计空间，写完冻结）
奖励组装 = 设计层：通过率折算方式 / 格式分 / 效率分 / 超时惩罚（★ 设计点全在这）
```

**时序解耦**：判分器是基础设施（D2 必须先做——筛 2 和 RL 打分都依赖它），写完过单测后**冻结**；整形层在其输出之上做算术，**D3/D4 之前任何时刻敲定都行，改动零成本**——这就是"奖励函数先不敲定、先跑通"的成立依据。

**判分器模块表**（总量约 100~200 行 Python，一两人一天）：

| 模块 | 干什么 | 规模 |
|---|---|---|
| `extract_python` | 从模型输出提取 ```python 块（正则 + 边界处理） | ~20 行 |
| `run_in_sandbox` | **两层设计**：基础版（subprocess + 超时，Windows 可用，本地筛 2 用）／增强版（+ rlimit 内存限制 + 断网，Linux，训练时执行模型生成的代码用——内容不可信需完整隔离） | ~50 行 |
| `score` | 按 §3 三层结构组装分数 | ~30 行 |
| 边界单测 | 100 条 case + 期望得分 | 验收件 |

---

## 3. Prompt 模板（一切开始前定稿，定稿后冻结）

```
Solve the following Python problem. Think briefly, then put your complete
implementation in a ```python code block.

Problem:
{question}
```

两条铁律：
1. **模板与判分器是双胞胎**——模板说"代码放 ```python 块"，判分器就按此提取；改模板措辞 = 改判分器提取规则，同定同冻；
2. **SFT 与 RL 必须同一份模板**（一字不差），否则格式 SFT 的收益作废。

> 待定决策（D2 会议上 10 分钟）：① 模板措辞终稿；② 提取规则细节（多个 ```python 块取第一个还是最后一个？）。

---

## 4. 清洗流水线（筛 1~4）

| 筛 | 做什么 | 在哪 | 淘汰规则 / 产出 |
|---|---|---|---|
| **筛 1** | 每条候选：`question` 填模板 → 生成正式 `prompt`；保留 `test` 字段 | 本地 | 产出中间池 1.5 万条 |
| **筛 2** | 官方 `solution` 存 `solution.py` + `test` 存测试文件 → 基础版沙箱实跑 | 本地 CPU | 淘汰：测试语法错 / 依赖缺失 / 单题 >10s / 测试为空；**预期通过 ≥95%** |
| **筛 3** | 题面去重；题面 >2000 字符弃用；`benchmark_similarity` >0.9 弃用（防泄漏） | 本地 | 产出候选 5~8k |
| **筛 4** | 基座（7B）每题采样 8 次（temp 1.0, max 768 tok）→ 判分 → pass rate | **服务器 1 卡**（7B 已就位并验证 ✅） | 保留 10%~90% → **`rl_pool.jsonl` 2~4k 条** + `heldout.jsonl` 200 条 |

**筛 2 为什么不需要模型**：它验证的是"测试本身能不能跑"——用官方解答（已知正确的代码）当考生，官方解答做不满分 = 卷子有毛病 = 淘汰。类比：新硬盘先全盘读写一遍，不是不信厂家，是廉价保险。

---

## 5. D2 人工任务清单（本周不需要写代码就能推进的活）

| # | 任务 | 性质 | 产出 |
|---|---|---|---|
| 1 | **⭐ 手工单题全流程**：挑 3 道候选题，人工走完"填模板 → solution.py → test 文件 → pytest 实跑 assert" | 理解活（最先做，一小时，做完全组对流水线恍然大悟） | 流程验证笔记 |
| 2 | 定稿 prompt 模板 + 提取规则（§3 的两个待定决策） | 设计活 | 模板终稿进 git |
| 3 | 定 `rl_pool.jsonl` 字段规范 | 接口设计 | 字段表（数据线与判分器线共同遵守） |
| 4 | 判分器 100 条边界 case 清单：无代码块 / 多代码块 / 只有思考没代码 / 语法错 / 死循环 / 超时 / 答非所问语言 / 缺 import……每条标注期望得分 | 选题活 | 判分器验收标准 |
| 5 | 沙箱方案定稿（两层设计的参数：超时 10s？内存上限？并发几路？） | 方案活 | 一页设计记录 |
| 6 | 评测集对齐：读 `test6.jsonl` 字段结构，定抽 200~300 题的规则与判分字段 | 体力活 | 评测抽取规则 |
| 7 | 外部依赖跟进：新镜像（管理员）/ CANN 包上传 | 沟通 | —— |

---

## 6. 验收清单（D2 收工标准）

- [ ] `rl_pool.jsonl` 2~4k 条，字段齐全（prompt / tests / meta: subset, style, pass_rate）
- [ ] 全池 100% 可被判分器执行（筛 2 复跑零失败）
- [ ] 随机 30 条人工检查：题面清晰、测试合理
- [ ] `heldout.jsonl` 200 条已隔离，绝不进训练
- [ ] 判分器单测对齐 ≥95%
- [ ] 评测子集就位（LCB 200~300 题 + MBPP+/HumanEval+）
- [ ] 所有脚本进 git，`rl_pool.jsonl` 生成过程可一键复现

---

## 7. 上传时序（本机 → 平台）

| 时点 | 上传 | 大小 |
|---|---|---|
| 已完成 | 两个模型 + mbpp（`mbpp_list` 文件夹） | ~17GB |
| 筛 1~3 跑完后 | `verified_pool.jsonl`（5~8k）→ 放 `project` 挂载 | 几十 MB |
| 筛 4 跑完后 | `rl_pool.jsonl` + `heldout.jsonl` 直接在服务器生成，**拉回本地归档** | 几十 MB |

**不上传**：原始 parquet、candidates 大文件、完整 LCB。原始数据留本地做可复现性存档。

**持久性纪律**：`/data/home/<你的学号>/` 父目录不持久，只有逐个挂载的子文件夹持久——建作业时用「添加挂载点」单独加 `project` 文件夹；容器内一切要保留的东西只写挂载路径。

---

## 附录：已执行过的下载命令存档（复现用）

```powershell
$env:HF_ENDPOINT = "https://hf-mirror.com"
$env:HF_HUB_DISABLE_XET = "1"   # 必加：Xet 后端走不了镜像（401）

hf download google-research-datasets/mbpp --repo-type dataset --include "*sanitized*" --local-dir data\mbpp
hf download livecodebench/code_generation_lite --repo-type dataset --include "test6.jsonl" --local-dir benchmark\lcb
hf download evalplus/mbppplus --repo-type dataset --local-dir benchmark\mbppplus
hf download evalplus/humanevalplus --repo-type dataset --local-dir benchmark\hepplus
# KodCode-V1：--include 多值必须一个一个重复写（连写会被 CLI 误解析）
hf download KodCode/KodCode-V1 --repo-type dataset --include "data/train-00005-of-00015.parquet" --include "data/train-00010-of-00015.parquet" --local-dir data\kodcode_v1
```
